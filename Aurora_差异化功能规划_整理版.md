# Aurora 差异化功能规划（逻辑整理版）

> **来源说明**：原始文本为用户提供的 OCR 提取结果，由**两张截图拼接**而成，存在标题重复、条目被标题切断、表格截断、AI 结尾语混入等问题。
> **本文档做的事**：① 还原被 OCR 打乱的结构；② 标注原文的截断处；③ 指出规划本身的逻辑缺口。**未增删任何功能主张。**

---

## 一、目标定位

Aurora 不应继续以「更多模型、更多工具、更多面板」为主要竞争点——这些能力已经成为 AI 编程 Agent 的常规配置。

更有辨识度的方向是：

> 构建面向真实代码仓库的**可验证** AI 编程 Agent，通过**影响分析、反事实实验和风险感知执行**，为每项代码改动提供**必要性、正确性与安全性证据**。

围绕三个关键词建设：

- **可验证**：不能只声称任务完成，必须提供测试、构建、影响范围和改动必要性证据。
- **可控制**：根据需求歧义、修改风险和回滚成本，动态决定自动执行或请求用户确认。
- **可评估**：Skill、策略和 Agent 行为必须经过对照评测，证明有效后才能启用。

---

## 二、第一梯队：最有差异化的功能（6 项）

| # | 功能 |
|---|---|
| 1 | 携带证据的代码补丁 |
| 2 | 反事实补丁验证 |
| 3 | Skill / Plugin / MCP 供应链安全 |
| 4 | 代码库隐式行为契约挖掘 |
| 5 | 不确定性感知的自主权限 |
| 6 | Skill 效果评测与准入 |

### 1. 携带证据的代码补丁

普通 Coding Agent 通常只输出代码和 Diff。Aurora 可以让每次改动同时附带一份结构化证据报告。

**核心能力**
- 将用户需求拆分为可验证的验收项
- 建立「需求 — 代码改动 — 验证结果」的关联关系
- 说明每处修改为什么必要
- 展示受影响的函数、调用方和关联测试
- 保存验证命令、退出状态和关键输出
- 明确标注尚未验证的内容和剩余风险

**最终输出**
- 代码 Diff
- 需求覆盖情况
- 修改必要性说明
- 调用影响范围
- 测试与构建证据

**与 Aurora 现有能力的结合**
- Guard：将自然语言需求转化为约束
- 结构索引：提供调用关系和影响范围
- Checkpoint：在验证失败时恢复文件
- 安全扫描：提供改动后的安全证据
- 失败归因：解释验证失败的原因

**价值**
把 Aurora 从「会修改代码的 Agent」提升为「能够解释并证明自己为何这样修改的 Agent」。

### 2. 反事实补丁验证

不仅验证「加入修改后测试是否通过」，还验证「**移除某项修改后问题是否重新出现**」。

**核心流程**
1. 将补丁拆分为多个语义改动单元
2. 逐项撤销、替换或重新组合改动
3. 在隔离环境中重新执行测试和验证
4. 判断每项修改是否对任务结果产生实际作用
5. 标记必要修改、冗余修改和高风险修改
6. 在多个可行方案中选择改动更小、风险更低的版本

**示例结论表**

| 改动 | 结论 | 验证依据 |
|---|---|---|
| 增加参数 | 必要 | 撤销后目标测试失败 |
| （待补） | （待补） | （待补） |

> ⚠️ **原文本此处被 OCR 截断**，只保留到第一行第三列「撤销后目标测」。
> 表头为「改动 / 结论 / 验证依据」，一行实测数据不构成示例，**需要补齐真实实验数据后才有说服力**。

### 3. Skill / Plugin / MCP 供应链安全

**导入前检查**
- 扫描 Skill 说明、引用资料、脚本和资源文件
- 检测远程脚本直接执行
- 检测敏感环境变量、密钥和用户目录读取
- 检测动态 Shell、破坏性删除和权限扩大
- 检测代码、日志或凭据外传行为
- 检测 Prompt Injection 和隐藏指令
- 检查依赖是否固定版本
- 记录来源、版本和内容摘要

**权限清单**（为每个扩展生成类似应用权限的声明）
- 是否读取工作区
- 是否修改文件
- 是否执行 Shell
- 是否访问网络
- 是否读取环境变量
- 是否安装依赖
- 是否调用远程 MCP
- 是否执行数据库写入

**运行时控制**
- 高风险权限必须由用户确认
- 首次运行使用受限模式
- 脚本只能通过 Aurora 的安全执行入口
- 保存每次权限使用记录
- 扩展内容发生变化后重新审批

**价值**
可形成独立亮点：为 AI Agent 扩展生态构建供应链安全机制，对 Skill、Plugin 和 MCP 配置进行静态扫描、权限分析、风险分级与运行时审计。

### 4. 代码库隐式行为契约挖掘

真实项目存在大量没有写进需求和文档的隐式约定。Agent 即使通过现有测试，也可能破坏这些行为。

**可挖掘的契约**
- 失败时返回特定结果，而不是抛出异常
- 删除操作必须使用软删除
- 配置更新后必须清除缓存
- 公共接口必须保持向后兼容
- 数据库写入必须同时生成审计记录
- 某些操作必须保持幂等
- 某些函数必须按照固定顺序调用

**信息来源**
调用关系 / 测试断言 / 类型声明 / 异常处理 / 相似模块 / 历史修改 / 运行时轨迹 / 项目文档

**执行方式**
1. 修改前提取候选行为契约
2. 区分强证据契约和推测契约
3. 为关键契约生成特征测试
4. 修改后重新验证
5. 无法确认的契约展示给用户，而不是擅自决定

**价值**
普通 RAG 解决「相关代码在哪里」，行为契约挖掘进一步解决「**哪些既有行为不能改变**」。

### 5. 不确定性感知的自主权限

不再使用固定的「全自动」或「所有操作都询问」，而是根据不确定性和潜在损失动态调整 Agent 权限。

**风险信号**
- 用户需求存在多种合理解释
- 无法唯一定位目标符号
- 调用图缺失或已经过期
- 改动涉及公共接口
- 缺少关联测试
- 涉及数据库、权限、安全或依赖升级
- 修改文件数量或影响范围超出预算
- 回滚成本较高

**行为策略**
- 低风险、高置信度：自动执行
- 中风险：先生成计划和影响范围，再执行
- 高风险：在关键决策点请求用户确认
- 无法可靠判断：停止修改并说明缺少的信息

**高质量澄清**
不询问笼统的「是否继续」，而是提出能够消除歧义的问题：

> 当前可以保留旧接口并增加适配层，也可以修改所有调用方。是否需要保持向后兼容？

**价值**
该功能可以把影响分析、安全审批和用户交互统一为一套**风险驱动的执行策略**。

> 📌 **原文此处被第二个标题切断**，「统一为一套风险驱动的」后面的话被挤到后文（原文以「## 价值」开头的那段）。以上已复原。

### 6. Skill 效果评测与准入

Skill 不应在创建或导入后立即启用，而应先证明它能够改善 Agent 表现。

**评测流程**
1. 选择一组历史任务或标准任务
2. 运行不使用 Skill 的基线组
3. 运行启用 Skill 的实验组
4. 对比任务成功率和执行成本
5. 检查是否产生新的安全问题或无关修改
6. 指标改善后才正式启用
7. 后续表现下降时回滚到旧版本

**建议指标**
任务成功率 / 首次修复成功率 / Token 消耗 / 工具调用次数 / 重复文件读取率 / 无关改动比例 / 安全违规数量 / 平均执行时间 / 用户介入次数

**与普通自学习的区别**
普通长期记忆侧重「记住经验」，该机制侧重：

> 先通过对照实验证明经验有效，再允许它改变 Agent 行为。

---

## 三、第二梯队：实用且适合展示的功能（4 项）

| # | 功能 | 定位 |
|---|---|---|
| 7 | 多方案补丁竞争 | 多候选补丁基于统一指标竞争 |
| 8 | 前端视觉回归验证 | 基于真实渲染结果验收 |
| 9 | 数据库变更安全模式 | 强化后端与安全方向定位 |
| 10 | 可回滚依赖升级 | 符合真实项目维护需求 |

### 7. 多方案补丁竞争

多个子 Agent 在隔离分支中生成不同方案：最小改动方案 / 向后兼容方案 / 结构优化方案 / 安全优先方案。

系统分别运行测试、安全扫描和影响分析，再由独立评审 Agent 根据证据选择最佳方案。

> **重点不在「同时启动多个 Agent」，而在**：多个候选补丁基于统一指标竞争，并由独立验证结果决定最终方案。

### 8. 前端视觉回归验证

前端修改完成后自动执行：
- 修改前后截图
- 桌面端和移动端视口检查
- Console 错误检测
- 空白页面和错误遮罩检测
- 关键用户流程交互
- 截图差异分析
- 视觉验证报告

> 构建通过不等于页面正常，该功能要求 Agent 基于真实渲染结果完成验收。

### 9. 数据库变更安全模式

数据库任务进入专用工作流：
- 分析迁移历史
- 检查权限与行级安全策略
- 检查索引和全表扫描风险
- 分析锁表和停机风险
- 生成回滚方案
- 在临时数据库验证
- 生产执行前强制审批
- 验证迁移后的结构和数据完整性

> 该能力实用性很强，适合强化后端和安全方向的项目定位。

### 10. 可回滚依赖升级

依赖升级不再一次性完成，而是：
1. 分析版本差异和破坏性变更
2. 生成分阶段升级计划
3. 每批只升级少量依赖
4. 每阶段保存 Checkpoint
5. 执行测试、构建和安全扫描
6. 定位引入回归的具体依赖
7. 失败时只回滚对应升级批次

> 该功能比简单修改依赖版本号更符合真实项目维护需求。

---

## 四、推荐建设顺序

### 第一阶段：建立可信交付闭环

1. 携带证据的代码补丁
2. **完成前验证门禁**（注：此项不在前文功能清单中，属路线图新增）
3. Skill、Plugin 与 MCP 供应链安全准入

> 目标：让 Aurora 的每次修改都有证据，让第三方扩展在启用前经过安全审查。

### 第二阶段：形成核心差异化

1. 反事实补丁验证
2. 不确定性感知的自主权限
3. 多方案补丁竞争

> 目标：让 Agent 不仅能生成代码，还能判断哪种修改更必要、更安全，以及何时不应该自行决定。

### 第三阶段：建立评测和进化体系

1. 行为契约挖掘
2. Skill 效果评测与准入
3. **策略版本管理和自动回滚**（注：此项不在前文功能清单中，属路线图新增）

> 目标：让 Agent 的能力升级建立在可重复评测结果上，而不是依赖主观判断。

---

## 五、最推荐的功能组合（资源有限时集中做这 5 项）

1. 携带证据的代码补丁
2. 反事实补丁验证
3. 不确定性感知的自主权限
4. Skill、Plugin 与 MCP 供应链安全准入
5. Skill 效果评测与准入

**这五项形成的闭环：**

```
修改前评估风险和隐式约束
  → 决定是否需要用户确认
  → 生成代码补丁
  → 通过反事实实验验证改动必要性
  → 输出证据化交付报告
  → 对有效策略进行对照评测
  → 评测通过后才正式启用
```

---

## 六、可用于简历的未来表述

> 设计面向真实代码仓库的可验证 AI 编程 Agent，通过结构化影响分析、反事实补丁实验和风险感知执行，为代码修改生成需求覆盖、改动必要性、测试验证及安全风险证据；同时构建 Skill、Plugin 与 MCP 扩展的供应链安全和效果准入机制。

**建议避免在没有实验数据前使用：**

- ❌ 首创
- ❌ 完全解决
- ❌ 准确识别所有风险
- ❌ 保证补丁正确
- ❌ 自动找到全局最优改动
- ❌ 显著提升成功率

**更稳妥的表达：**

- ✅ 设计并实现
- ✅ 构建验证机制
- ✅ 通过实验识别
- ✅ 降低风险
- ✅ 提供证据
- ✅ 支持回滚

> ⚠️ **注意**：本节标题是「可用于**未来**的表述」。这些功能**尚未实现**，不能直接写进现有简历。

---

## 七、最终建议

Aurora 最值得发展的方向不是功能数量，而是**可信程度**。

> 其他 Coding Agent 负责生成代码，Aurora 进一步负责解释为什么这样修改、证明哪些修改确实必要、识别什么时候不应自行执行，并验证引入的新风险。

---

# 附录：原文的逻辑缺口（整理时发现）

## A. OCR 造成的结构错乱（已修复）

| 问题 | 说明 |
|---|---|
| **标题重复** | 「Aurora_差异化功能规划」与「Aurora 差异化功能规范（完整整理版）」实为同一份文档，第二个标题是被切开的位置 |
| **条目被切断** | 功能 5「不确定性感知的自主权限」的价值句被第二个标题截断，后半句跑到后文 |
| **功能 6 错位** | 「Skill 效果评测与准入」被切到了第二个标题之下，实际属于**第一梯队第 6 项** |
| **表格截断** | 功能 2 的示例结论表只保留一行且末列残缺（「撤销后目标测」） |
| **AI 结尾语混入** | 「如果你需要，我可以再帮你压缩成一页 PPT 提纲…」出现两次，是生成该文档的 AI 助手的收尾话术，不属于文档内容 |

## B. 规划本身的逻辑缺口（**需要你决定怎么补**）

| # | 缺口 | 说明 |
|---|---|---|
| 1 | **功能 8/9/10 在路线图中消失** | 清单列了 10 项，但「推荐建设顺序」只排了 7 项。**前端视觉回归、数据库变更安全、可回滚依赖升级**从未出现在任何阶段 |
| 2 | **路线图凭空多出 2 项** | 「完成前验证门禁」（第一阶段）、「策略版本管理和自动回滚」（第三阶段）**不在前面的 10 项清单里**，来源不明 |
| 3 | **「最推荐 5 项」与「第一梯队 6 项」不一致** | 第一梯队有 6 项，重点推荐挑了 5 项，**漏掉的是第 4 项「行为契约挖掘」**，但未说明为何排除 |
| 4 | **关键词 3 个 vs 结论 4 个维度** | 开头定义「可验证 / 可控制 / 可评估」三个关键词；结尾却说「解释为什么（可解释）、证明必要（可验证）、识别何时不自执行（可控制）、验证风险」，隐含了第 4 个维度 |
| 5 | **术语不统一** | 「Skill/MCP 供应链安全」/「Skill、Plugin 与 MCP 供应链安全准入」/「Skill、Plugin 与 MCP 扩展的供应链安全」三种写法混用 |

## C. 与 Aurora 现状的对照

文档中「与现有能力结合」提到的模块**均已存在**：

| 文档提到 | Aurora 实际位置 | 状态 |
|---|---|---|
| Guard | `backend/necessity/guard/` | ✅ 已实现 |
| 结构索引 | `backend/necessity/index/` | ✅ 已实现 |
| Checkpoint | `backend/agent/checkpoint.py` | ✅ 已实现（含真实文件回滚） |
| 安全扫描 | `backend/security_scanner.py` | ✅ 已实现 |
| 失败归因 | `backend/necessity/attribution/` | ✅ 已实现 |
| 评测框架 | `backend/necessity/eval/` | ✅ 已实现（含 Gate 0、任务集、P/R） |

> **这意味着**：功能 1「携带证据的代码补丁」**基本是把现有模块组装起来**，而非从零开发。
> 同理，功能 2「反事实补丁验证」所需的隔离环境与测试执行能力，`necessity/eval/` 已有基础。
